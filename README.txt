EHBO cleanup batch 6

Replace:
  assets/css/informatie.css

What changed:
- Removed obsolete Documents styling that no longer has matching markup.
- Removed the old Membership CTA generation that is no longer used.
- Removed the old final Informatie CTA generation that is no longer used.
- Removed their leftover responsive rules while preserving the live selectors in shared media-query groups.

No current page-informatie.php markup uses these deleted class names.
